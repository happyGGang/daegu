<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.whalesoft.app.cms.workingLog.WorkingLogDao">


	<insert id="addWorkingLog">
		INSERT INTO
			WB_WORKING_LOG
		(
			work_idx, site_id, work_type, work_comment, work_command,
			work_query, work_result_count, work_date, work_ip, member_id,
			work_reason, work_result
		)
		VALUES
		(
			WB_WOKING_LOG_IDX_SEQ.nextval, #{site_id}, #{work_type}, #{work_comment}, #{work_command},
			#{work_query}, #{work_result_count}, SYSDATE, #{work_ip}, #{member_id},
			#{work_reason}, #{work_result}
		)

		<selectKey keyProperty="work_idx" resultType="Integer" order="AFTER">
			SELECT WB_WOKING_LOG_IDX_SEQ.currval FROM dual
		</selectKey>
	</insert>

	<sql id="where">
		<trim prefix="WHERE" prefixOverrides="AND">
			<if test="site_id != null and site_id != ''">
			AND	site_id = #{site_id}
			</if>
			<if test="work_command != null and work_command != ''">
			AND	work_command = #{work_command}
			</if>
			<if test="work_type != null and work_type != ''">
			AND	work_type = #{work_type}
			</if>
			<if test="search_text != null and search_text != ''">
			AND	${search_type} LIKE '%' || #{search_text} || '%'
			</if>
		</trim>
	</sql>

	<select id="getWorkingLogCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			WB_WORKING_LOG
		<include refid="where" />
	</select>

	<select id="getWorkingLogList" resultType="kr.co.whalesoft.app.cms.workingLog.WorkingLog">
		SELECT
			A.*,
			(SELECT NVL(homepage_alias, homepage_name) FROM CMS_HOMEPAGE B where b.homepage_id = a.site_id) AS siteName
		FROM
			(
			${pagingTop}
			SELECT
				work_idx, site_id, work_type, work_comment, work_command,
				work_result_count, work_date, work_ip, member_id, work_reason
			FROM
				WB_WORKING_LOG
			<include refid="where" />
			ORDER BY
				work_idx DESC
			${pagingBottom}
			) A
	</select>
	
	<select id="getWorkingLogExcelList" resultType="kr.co.whalesoft.app.cms.workingLog.WorkingLog">
		SELECT
			A.*,
			(SELECT NVL(homepage_alias, homepage_name) FROM CMS_HOMEPAGE B where b.homepage_id = a.site_id) AS siteName
		FROM
			(
			SELECT
				work_idx, site_id, work_type, work_comment, work_command,
				work_result_count, work_date, work_ip, member_id, work_reason
			FROM
				WB_WORKING_LOG
			<include refid="where" />
			ORDER BY
				work_idx DESC
			) A
	</select>

	<select id="getWorkingLogOne" resultType="kr.co.whalesoft.app.cms.workingLog.WorkingLog">
		SELECT
			work_idx, site_id, work_type, work_comment, work_command,
			work_query, work_result_count, work_date, work_ip, member_id,
			work_reason, work_result,
			(SELECT NVL(homepage_alias, homepage_name) FROM CMS_HOMEPAGE B where b.homepage_id = a.site_id) AS siteName
		FROM
			WB_WORKING_LOG A
		WHERE
			work_idx = #{work_idx}
	</select>
</mapper>