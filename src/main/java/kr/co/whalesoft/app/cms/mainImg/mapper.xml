<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.whalesoft.app.cms.mainImg.MainImgDao">

	<select id="getMainImgListAll" resultType="kr.co.whalesoft.app.cms.mainImg.MainImg">
			SELECT
				homepage_id, img_idx, main_img_name, org_file_name, server_file_name,
				file_extension, file_size, use_yn, print_seq, add_date,
				alt_text
			FROM
				CMS_MAIN_IMG
			WHERE
				homepage_id = #{homepage_id}
			AND	use_yn = 'Y'
			<if test="search_type != null and search_type != '' and search_text != null and search_text != ''">
			AND	${search_type} LIKE '%'||#{search_text}||'%'
			</if>
			ORDER BY
				print_seq ASC, add_date DESC
	</select>

	<select id="getMainImgList" resultType="kr.co.whalesoft.app.cms.mainImg.MainImg">
		${pagingTop}
			SELECT
				homepage_id, img_idx, main_img_name, org_file_name, server_file_name,
				file_extension, file_size, use_yn, print_seq, add_date,
				alt_text
			FROM
				CMS_MAIN_IMG
			WHERE
				homepage_id = #{homepage_id}
			<if test="search_type != null and search_type != '' and search_text != null and search_text != ''">
			AND	${search_type} LIKE '%'||#{search_text}||'%'
			</if>
			<if test="use_yn != null and use_yn != ''">
			AND	use_yn = #{use_yn}
			</if>
			ORDER BY
				print_seq ASC, add_date DESC
		${pagingBottom}
	</select>

	<select id="getMainImgListCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			CMS_MAIN_IMG
		WHERE
			homepage_id = #{homepage_id}
		<if test="search_type != null and search_type != '' and search_text != null and search_text != ''">
		AND	${search_type} LIKE '%'||#{search_text}||'%'
		</if>
		<if test="use_yn != null and use_yn != ''">
		AND	use_yn = #{use_yn}
		</if>
		ORDER BY
			print_seq ASC, add_date DESC
	</select>

	<select id="getMainImgOne" resultType="kr.co.whalesoft.app.cms.mainImg.MainImg">
		SELECT
			homepage_id, img_idx, main_img_name, org_file_name, server_file_name,
			file_extension, file_size, use_yn, print_seq, add_date,
			alt_text
		FROM
			CMS_MAIN_IMG
		WHERE
			homepage_id = #{homepage_id}
		AND	img_idx = #{img_idx}
	</select>

	<insert id="addMainImg">
		<selectKey keyProperty="img_idx" resultType="int" order="BEFORE">
			SELECT NVL(MAX(img_idx),0)+1 FROM CMS_MAIN_IMG WHERE homepage_id = #{homepage_id}
		</selectKey>

		INSERT INTO
			CMS_MAIN_IMG
		(
			homepage_id, img_idx, main_img_name, org_file_name, server_file_name,
			file_extension, file_size, use_yn, print_seq,
			add_id, add_date, alt_text
		) VALUES (
			#{homepage_id}, #{img_idx}, #{main_img_name}, #{org_file_name}, #{server_file_name},
			#{file_extension}, #{file_size}, #{use_yn}, #{print_seq},
			#{add_id}, SYSDATE, #{alt_text}
		)
	</insert>

	<update id="modifyMainImg">
		UPDATE
			CMS_MAIN_IMG
		SET
			main_img_name = #{main_img_name},
			<if test="server_file_name != null and server_file_name != ''">
			org_file_name = #{org_file_name},
			server_file_name = #{server_file_name},
			file_extension = #{file_extension},
			file_size = #{file_size},
			</if>
			use_yn = #{use_yn},
			print_seq = #{print_seq},
			alt_text = #{alt_text},
			modify_id = #{modify_id},
			modify_date = SYSDATE
		WHERE
			homepage_id = #{homepage_id}
		AND	img_idx = #{img_idx}
	</update>

	<delete id="deleteMainImg">
		DELETE FROM
			CMS_MAIN_IMG
		WHERE
			homepage_id = #{homepage_id}
		AND	img_idx = #{img_idx}
	</delete>

	<select id="getNextPrintSeq" resultType="int">
		SELECT
			NVL(MAX(print_seq), 0) + 10
		FROM
			CMS_MAIN_IMG
		WHERE
			homepage_id = #{homepage_id}
	</select>

</mapper>