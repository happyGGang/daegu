<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.whalesoft.app.cms.recommendSite.RecommendSiteDao">

	<select id="getRecommendSiteListAll" resultType="kr.co.whalesoft.app.cms.recommendSite.RecommendSite">
		SELECT
			homepage_id, recommend_site_idx, recommend_site_name, recommend_site_desc, link_target,
			print_seq, add_date
		FROM
			CMS_RECOMMEND_SITE
		WHERE
			homepage_id = #{homepage_id}
		<if test="search_type != null and search_type != '' and search_text != null and search_text != ''">
		AND	${search_type} LIKE '%'||#{search_text}||'%'
		</if>
		ORDER BY
			print_seq ASC
	</select>

	<select id="getRecommendSiteListCache" resultType="kr.co.whalesoft.app.cms.recommendSite.RecommendSite">
		SELECT
			homepage_id, recommend_site_idx, recommend_site_name, recommend_site_desc, link_target,
			print_seq, add_date
		FROM
			CMS_RECOMMEND_SITE
		WHERE
			homepage_id = #{value}
		ORDER BY
			print_seq ASC
	</select>

	<select id="getRecommendSiteList" resultType="kr.co.whalesoft.app.cms.recommendSite.RecommendSite">
		${pagingTop}
		SELECT
			homepage_id, recommend_site_idx, recommend_site_name, recommend_site_desc, link_target,
			print_seq, add_date
		FROM
			CMS_RECOMMEND_SITE
		WHERE
			homepage_id = #{homepage_id}
		<if test="search_type != null and search_type != '' and search_text != null and search_text != ''">
		AND	${search_type} LIKE '%'||#{search_text}||'%'
		</if>
		ORDER BY
			print_seq ASC
		${pagingBottom}
	</select>

	<select id="getRecommendSiteListCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			CMS_RECOMMEND_SITE
		WHERE
			homepage_id = #{homepage_id}
		<if test="search_type != null and search_type != '' and search_text != null and search_text != ''">
		AND	${search_type} LIKE '%'||#{search_text}||'%'
		</if>
		ORDER BY
			print_seq ASC
	</select>

	<select id="getRecommendSiteOne" resultType="kr.co.whalesoft.app.cms.recommendSite.RecommendSite">
		SELECT
			homepage_id, recommend_site_idx, recommend_site_name, recommend_site_desc, link_target,
			print_seq, add_date
		FROM
			CMS_RECOMMEND_SITE
		WHERE
			homepage_id = #{homepage_id}
		AND	recommend_site_idx = #{recommend_site_idx}
	</select>

	<insert id="addRecommendSite">
		<selectKey keyProperty="recommend_site_idx" resultType="int" order="BEFORE">
			SELECT NVL(MAX(recommend_site_idx),0)+1 FROM CMS_RECOMMEND_SITE WHERE homepage_id = #{homepage_id}
		</selectKey>

		INSERT INTO CMS_RECOMMEND_SITE
		(
			homepage_id, recommend_site_idx, recommend_site_name, recommend_site_desc, link_target,
			print_seq, add_id, add_date
		)
			VALUES
		(
			#{homepage_id}, #{recommend_site_idx}, #{recommend_site_name}, #{recommend_site_desc}, #{link_target},
			#{print_seq}, #{add_id}, SYSDATE
		)
	</insert>

	<update id="modifyRecommendSite">
		UPDATE
			CMS_RECOMMEND_SITE
		SET
			recommend_site_name = #{recommend_site_name},
			recommend_site_desc = #{recommend_site_desc},
			link_target = #{link_target},
			print_seq = #{print_seq},
			modify_id = #{modify_id},
			modify_date = SYSDATE
		WHERE
			homepage_id = #{homepage_id}
		AND	recommend_site_idx = #{recommend_site_idx}
	</update>

	<delete id="deleteRecommendSite">
		DELETE
			CMS_RECOMMEND_SITE
		WHERE
			homepage_id = #{homepage_id}
		AND	recommend_site_idx = #{recommend_site_idx}
	</delete>
	
	<select id="getNextPrintSeq" resultType="int">
		SELECT
			NVL(MAX(print_seq), 0) + 10
		FROM
			CMS_RECOMMEND_SITE
		WHERE
			homepage_id = #{homepage_id}
	</select>

</mapper>