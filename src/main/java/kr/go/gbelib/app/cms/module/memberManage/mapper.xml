<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.cms.module.memberManage.MemberManageDao">

	<select id="getMemberManageList" resultType="kr.go.gbelib.app.cms.module.memberManage.MemberManage">
		${pagingTop}
		SELECT
			member_manage_idx, member_name, member_id, member_password, school,
			last_connect, add_id, add_date
		FROM
			MEMBER_MANAGE
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="(search_type != null and search_type != '') and (search_text != '' and search_text != '')">
			AND	${search_type} LIKE '%'||#{search_text}||'%'
			</if>
		</trim>
		${pagingBottom}
	</select>
	
	<select id="getMemberManageCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			MEMBER_MANAGE
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="(search_type != null and search_type != '') and (search_text != '' and search_text != '')">
			AND	${search_type} LIKE '%'||#{search_text}||'%'
			</if>
		</trim>
	</select>
	
	<select id="getMemberManageOne" resultType="kr.go.gbelib.app.cms.module.memberManage.MemberManage">
		SELECT
			member_manage_idx, member_name, member_id, member_password, school,
			last_connect, add_id, add_date
		FROM
			MEMBER_MANAGE
		WHERE
			member_manage_idx = #{member_manage_idx}
	</select>
	
	<select id="memberIdDuplCheck" resultType="int">
		SELECT
			COUNT(*)
		FROM
			MEMBER_MANAGE
		WHERE
			member_id = #{member_id}
	</select>
	
	<insert id="addMemberManage">
		<selectKey keyProperty="member_manage_idx" resultType="int" order="BEFORE">
			SELECT NVL(MAX(member_manage_idx), 0) + 1 FROM MEMBER_MANAGE
		</selectKey>
		INSERT INTO
			MEMBER_MANAGE
		(
			member_manage_idx, member_name, member_id, member_password, school,
			last_connect, add_id, add_date
		)
		VALUES
		(
			#{member_manage_idx}, #{member_name}, #{member_id}, #{member_password}, #{school},
			#{last_connect}, #{add_id}, SYSDATE
		)
	</insert>
	
	<update id="modifyMemberManage">
		UPDATE
			MEMBER_MANAGE
		SET
			member_name = #{member_name},
			member_id = #{member_id},
			<if test="member_password != null and member_password != ''">
			member_password = #{member_password},
			</if>
			school = #{school},
			modify_id = #{modify_id},
			modify_date = SYSDATE
		WHERE
			member_manage_idx = #{member_manage_idx}
	</update>
	
	<delete id="deleteMemberManage">
		DELETE FROM
			MEMBER_MANAGE
		WHERE
			member_manage_idx = #{member_manage_idx}
	</delete>
	
	<delete id="deleteCheckMemberManage">
		DELETE FROM
			MEMBER_MANAGE
		WHERE
			member_manage_idx IN
			<foreach collection="member_manage_arr" open="(" close=")" separator="," item="i">
			#{i}
			</foreach>
	</delete>
	
</mapper>