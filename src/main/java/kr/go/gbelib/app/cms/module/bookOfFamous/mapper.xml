<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.cms.module.bookOfFamous.BookOfFamousDao">

	<select id="getBookOfFamousCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			BOOK_OF_FAMOUS
		WHERE
			homepage_id = #{homepage_id}
		AND delete_yn = 'N'
		<if test="search_type != null and search_type != ''">
		AND	${search_type} LIKE '%'|| #{search_text} ||'%'
		</if>
	</select>

	<select id="getBookOfFamousList" resultType="kr.go.gbelib.app.cms.module.bookOfFamous.BookOfFamous">
		SELECT
			book_famous_idx, homepage_id, notice_yn, selection_year, famous_name,
			book_name, book_author, book_publisher, book_year, book_regno,
			book_isbn, book_img_url, book_content, top_html, bottom_html,
			add_id, add_date, modify_id, modify_date, delete_yn
		FROM
			BOOK_OF_FAMOUS
		WHERE
			homepage_id = #{homepage_id}
		AND delete_yn = 'N'
		<if test="search_type != null and search_type != ''">
		AND	${search_type} LIKE '%'|| #{search_text} ||'%'
		</if>
		ORDER BY
			(CASE WHEN notice_yn = 'Y' THEN 1 ELSE 2 END), add_date DESC
	</select>

	<select id="getBookOfFamousOne" resultType="kr.go.gbelib.app.cms.module.bookOfFamous.BookOfFamous">
		SELECT
			book_famous_idx, homepage_id, notice_yn, selection_year, famous_name,
			book_name, book_author, book_publisher, book_year, book_regno,
			book_isbn, book_img_url, book_content, top_html, bottom_html,
			add_id, add_date, modify_id, modify_date, delete_yn
		FROM
			BOOK_OF_FAMOUS
		WHERE
			homepage_id = #{homepage_id}
		AND delete_yn = 'N'
		AND	book_famous_idx = #{book_famous_idx}
	</select>

	<insert id="addBookOfFamous">
		<selectKey keyProperty="book_famous_idx" resultType="int" order="BEFORE">
			SELECT NVL(MAX(book_famous_idx), 0) + 1 FROM BOOK_OF_FAMOUS
		</selectKey>
		INSERT INTO
			BOOK_OF_FAMOUS
		(
			book_famous_idx, homepage_id, notice_yn, selection_year, famous_name,
			book_name, book_author, book_publisher, book_year, book_regno,
			book_isbn, book_img_url, book_content, top_html, bottom_html,
			add_id, add_date, delete_yn
		)
			VALUES
		(
			#{book_famous_idx}, #{homepage_id}, #{notice_yn}, #{selection_year}, #{famous_name},
			#{book_name}, #{book_author}, #{book_publisher}, #{book_year}, #{book_regno},
			#{book_isbn}, #{book_img_url}, #{book_content}, #{top_html}, #{bottom_html},
			#{add_id}, SYSDATE, 'N'
		)

	</insert>

	<update id="modifyBookOfFamous">
		UPDATE
			BOOK_OF_FAMOUS
		SET
			notice_yn = #{notice_yn},
			famous_name = #{famous_name},
			book_name = #{book_name},
			book_author = #{book_author},
			book_publisher = #{book_publisher},
			book_year = #{book_year},
			book_regno = #{book_regno},
			book_isbn = #{book_isbn},
			book_img_url = #{book_img_url},
			book_content = #{book_content},
			top_html = #{top_html},
			bottom_html = #{bottom_html},
			modify_id = #{modify_id},
			modify_date = SYSDATE
		WHERE
			homepage_id = #{homepage_id}
		AND	book_famous_idx = #{book_famous_idx}
	</update>

	<update id="deleteBookOfFamous">
		UPDATE
			BOOK_OF_FAMOUS
		SET
			delete_yn = 'Y'
		WHERE
			homepage_id = #{homepage_id}
		AND	book_famous_idx = #{book_famous_idx}
	</update>
	
	<select id="getBookOfFamousListAPi" resultType="kr.go.gbelib.app.cms.module.bookOfFamous.BookOfFamous">
		SELECT
			book_famous_idx, homepage_id, notice_yn, selection_year, famous_name,
			book_name, book_author, book_publisher, book_year, book_regno,
			book_isbn, book_img_url, book_content, top_html, bottom_html,
			add_id, add_date, modify_id, modify_date, delete_yn
		FROM
			BOOK_OF_FAMOUS
		WHERE
			homepage_id = #{homepage_id}
		AND delete_yn = 'N'
		ORDER BY
			(CASE WHEN notice_yn = 'Y' THEN 1 ELSE 2 END), add_date DESC
	</select>

</mapper>