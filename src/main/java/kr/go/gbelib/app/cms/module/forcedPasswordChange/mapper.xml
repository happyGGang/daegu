<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.cms.module.forcedPasswordChange.ForcePasswordChangeDao">

	<insert id="addForcedPasswordChangeUser">
		INSERT INTO FORCED_PASSWORD_CHANGE
		(
			FORCED_PASSWORD_CHANGE_IDX, FORCED_PASSWORD_CHANGE_STATUS, MEMBER_ID, MEMBER_NAME,
		 	ADD_ID, ADD_DATE, DELETE_YN, REASON
		)
		VALUES
		(
			FORCED_PASSWORD_CHANGE_SEQ.NEXTVAL, #{forced_password_change_status}, #{member_id}, #{member_name},
			#{add_id}, SYSDATE, 'N', #{reason}
		)
	</insert>

	<update id="modifyForcedPasswordChangeUser">
		UPDATE
			FORCED_PASSWORD_CHANGE
		SET
			FORCED_PASSWORD_CHANGE_STATUS = #{forced_password_change_status},
			MEMBER_ID = #{member_id},
			MEMBER_NAME = #{member_name},
			MODIFY_ID = #{modify_id},
			REASON = #{reason},
			MODIFY_DATE = SYSDATE
		WHERE
			FORCED_PASSWORD_CHANGE_IDX = #{forced_password_change_idx}
	</update>

	<update id="deleteForcedPasswordChangeUser">
		UPDATE
			FORCED_PASSWORD_CHANGE
		SET
			DELETE_YN = 'Y'
		WHERE
			FORCED_PASSWORD_CHANGE_IDX = #{forced_password_change_idx}
	</update>

	<update id="updateForcedPasswordUserStatus">
		UPDATE
			FORCED_PASSWORD_CHANGE
		SET
			FORCED_PASSWORD_CHANGE_STATUS = 'Y',
			MODIFY_DATE = SYSDATE
		WHERE
			MEMBER_ID = #{member_id}
	</update>

	<select id="getForcedPasswordChangeList" resultType="kr.go.gbelib.app.cms.module.forcedPasswordChange.ForcedPasswordChange">
		${pagingTop}
			SELECT
				FORCED_PASSWORD_CHANGE_IDX, FORCED_PASSWORD_CHANGE_STATUS, MEMBER_ID, MEMBER_NAME, REASON,
				ADD_ID, ADD_DATE, MODIFY_ID, MODIFY_DATE, DELETE_YN
			FROM
				FORCED_PASSWORD_CHANGE
				WHERE
				    DELETE_YN = 'N'
		<if test=" search_type != null and search_type != '' and search_text != '' and search_text != '' ">
			AND	${search_type} LIKE '%'||#{search_text}||'%'
		</if>
		${pagingBottom}
	</select>

	<select id="getForcedPasswordChangeListCount" resultType="int">
		SELECT
		    COUNT(*)
		FROM
			FORCED_PASSWORD_CHANGE
		WHERE
			DELETE_YN = 'N'
		<if test=" search_type != null and search_type != '' and search_text != '' and search_text != '' ">
			AND	${search_type} LIKE '%'||#{search_text}||'%'
		</if>
	</select>

	<select id="getForcedPasswordChangeOne"	resultType="kr.go.gbelib.app.cms.module.forcedPasswordChange.ForcedPasswordChange">
		SELECT
			FORCED_PASSWORD_CHANGE_IDX, FORCED_PASSWORD_CHANGE_STATUS, MEMBER_ID, MEMBER_NAME, REASON,
			ADD_ID, ADD_DATE, MODIFY_ID, MODIFY_DATE, DELETE_YN
		FROM
			FORCED_PASSWORD_CHANGE
		WHERE
			FORCED_PASSWORD_CHANGE_IDX = #{forced_password_change_idx}
	</select>

	<select id="getForcedPasswordOneUser" resultType="int">
		SELECT
			COUNT(*)
		FROM
			FORCED_PASSWORD_CHANGE
		WHERE
			MEMBER_ID = #{member_id}
		AND
			DELETE_YN = 'N'
		AND
			FORCED_PASSWORD_CHANGE_STATUS = 'N'
	</select>
</mapper>