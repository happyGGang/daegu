<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.cms.module.checkInOut.CheckInOutDao">

	<sql id="selectFiled">
		homepage_id, checkInOut_idx, user_no, member_id, member_name,
		member_birth, member_sex, member_area, ROUND(checkInOut_time) AS checkInOut_time, check_type,
		TO_CHAR(checkIn_time,'yyyy-MM-dd HH24:MI:SS') AS checkIn_time, TO_CHAR(checkOut_time,'yyyy-MM-dd HH24:MI:SS') AS checkOut_time,
		gubun
	</sql>
	
	<sql id="where">
		<if test="start_date != null and start_date != '' and end_date != null and end_date != ''">
			AND TO_DATE(checkIn_time,'yyyy-MM-dd') BETWEEN #{start_date} AND #{end_date}
		</if>
		<if test="search_type != null and search_type != '' and search_text != null and search_text != ''">
			AND ${search_type} LIKE '%'||#{search_text}||'%'
		</if>
    </sql>
	
	<select id="getCheckInOutCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
			<include refid="where"/>
	</select>

	<select id="getCheckInOutList" resultType="kr.go.gbelib.app.cms.module.checkInOut.CheckInOut">
		${pagingTop}
		SELECT
			<include refid="selectFiled" />
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
			<include refid="where"/>
		ORDER BY
			checkInOut_idx DESC
		${pagingBottom}
	</select>
	
	<select id="getVisitCheck" resultType="int">
		SELECT
			COUNT(*)
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
		AND member_id = #{member_id}
	</select>
	
	<select id="isCheckInCount" resultType="boolean">
		SELECT
			COUNT(*)
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
		AND member_id = #{member_id}
		AND checkOut_time IS NULL
	</select>
	
	<select id="isCheckOutCount" resultType="boolean">
		SELECT
			COUNT(*)
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
		AND member_id = #{member_id}
		AND checkOut_time IS NULL
	</select>
	
	<select id="isCheckIn" resultType="kr.go.gbelib.app.cms.module.checkInOut.CheckInOut">
		SELECT
			<include refid="selectFiled" />
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
		AND member_id = #{member_id}
		AND checkOut_time IS NULL
	</select>
	
	<insert id="checkIn">
		<selectKey keyProperty="checkInOut_idx" resultType="int" order="BEFORE">
			SELECT NVL(MAX(checkInOut_idx), 0)+1 FROM CHECKINOUT
		</selectKey>
		INSERT INTO CHECKINOUT
		(
			homepage_id, checkInOut_idx, user_no, member_id, member_name,
			member_birth, member_sex, member_area, checkIn_time, checkOut_time,
			checkInOut_time, check_type, gubun
		)
		VALUES
		(
			#{homepage_id}, #{checkInOut_idx}, #{user_no}, #{member_id}, #{member_name},
			#{member_birth}, #{member_sex}, #{member_area}, SYSDATE, #{checkOut_time},
			#{checkInOut_time}, #{check_type}, '0'
		)
	</insert>
	
	<update id="checkOut">
		UPDATE
			CHECKINOUT
		SET
			checkOut_time = SYSDATE,
			checkInOut_time = (
								SELECT
									ROUND((SYSDATE - TO_DATE(#{checkIn_time}, 'YYYY-MM-DD HH24:MI:SS')) * 24 * 60, 2)
								FROM 
									dual
							   ),
			gubun = '1'
		WHERE
			homepage_id = #{homepage_id}
		AND	checkInOut_idx = #{checkInOut_idx}
	</update>
	
	<select id="getCheckOutTime" resultType="kr.go.gbelib.app.cms.module.checkInOut.CheckInOut">
		SELECT
			<include refid="selectFiled" />
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
		AND checkInOut_idx = #{checkInOut_idx}
	</select>
	
	<select id="getChartData" resultType="kr.go.gbelib.app.cms.module.checkInOut.CheckInOut">
		SELECT 
			RESULT_DATE, SUM(RESULT_COUNT) AS RESULT_COUNT
		FROM (
			SELECT
				TO_CHAR(checkIn_time, 'YYYY-MM-DD') AS result_date , '1' AS result_count
			FROM
				CHECKINOUT
		 	WHERE homepage_id = #{homepage_id}
		 	<if test="start_date != null and start_date != '' and end_date != null and end_date != ''">
				AND TO_CHAR(checkIn_time,'yyyy-MM-dd') BETWEEN #{start_date} AND #{end_date}
			</if>
			GROUP BY (TO_CHAR(checkIn_time, 'YYYY-MM-DD'), member_id)
		)
		GROUP BY result_date
		ORDER BY result_date ASC
	</select>
	
	<select id="getCheckInCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
		AND TO_CHAR(checkIn_time,'yyyy-MM-dd') = TO_CHAR(SYSDATE,'yyyy-MM-dd')
		AND checkOut_time IS NULL
	</select>
	
	<select id="getCheckOutCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
		AND TO_CHAR(checkIn_time,'yyyy-MM-dd') = TO_CHAR(SYSDATE,'yyyy-MM-dd')
		AND checkOut_time IS NOT NULL
	</select>
	
	<select id="getCheckInOutExcelList" resultType="kr.go.gbelib.app.cms.module.checkInOut.CheckInOut">
		SELECT
			<include refid="selectFiled" />
		FROM
			CHECKINOUT
		WHERE
			homepage_id = #{homepage_id}
			<include refid="where"/>
		ORDER BY
			checkInOut_idx DESC
	</select>
	
</mapper>