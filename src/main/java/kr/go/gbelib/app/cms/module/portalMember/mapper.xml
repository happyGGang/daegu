<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.cms.module.portalMember.PortalMemberDao">

	<select id="getPortalMemberList" resultType="kr.go.gbelib.app.cms.module.portalMember.PortalMember">
		${pagingTop}
		SELECT
			portal_member_idx, agency_name, agency_id, agency_password,
			auth_group, library_code, last_connect, add_id, add_date
		FROM
			PORTAL_MEMBER
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="(search_type != null and search_type != '') and (search_text != '' and search_text != '')">
			AND	${search_type} LIKE '%'||#{search_text}||'%'
			</if>
		</trim>
		ORDER BY add_date DESC
		${pagingBottom}
	</select>
	
	<select id="getPortalMemberCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			PORTAL_MEMBER
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="(search_type != null and search_type != '') and (search_text != '' and search_text != '')">
			AND	${search_type} LIKE '%'||#{search_text}||'%'
			</if>
		</trim>
	</select>
	
	<select id="getPortalMemberOne" resultType="kr.go.gbelib.app.cms.module.portalMember.PortalMember">
		SELECT
			portal_member_idx, agency_name, agency_id, agency_password,
			auth_group, library_code, last_connect, add_id, add_date
		FROM
			PORTAL_MEMBER
		<trim prefix="WHERE" prefixOverrides="AND|OR">
			<if test="portal_member_idx != 0">
			AND	portal_member_idx = #{portal_member_idx}
			</if>
			<if test="agency_id != null and agency_id != ''">
			AND	agency_id = #{agency_id}
			</if>
		</trim>
	</select>
	
	<insert id="addPortalMember">
		<selectKey keyProperty="portal_member_idx" resultType="int" order="BEFORE">
			SELECT NVL(MAX(portal_member_idx), 0) + 1 FROM PORTAL_MEMBER
		</selectKey>
		INSERT INTO
			PORTAL_MEMBER
		(
			portal_member_idx, agency_name, agency_id, agency_password,
			auth_group, library_code, add_id, add_date
		)
		VALUES
		(
			#{portal_member_idx}, #{agency_name}, #{agency_id}, #{agency_password},
			#{auth_group}, #{library_code}, #{add_id}, SYSDATE
		)
	</insert>
	
	<update id="modifyPortalMember">
		UPDATE
			PORTAL_MEMBER
		SET
			agency_name = #{agency_name},
			agency_id = #{agency_id},
			<if test="agency_password != null and agency_password != ''">
			agency_password = #{agency_password},
			</if>
			auth_group = #{auth_group},
			<if test="library_code != null and library_code != ''">
			library_code = #{library_code},
			</if>
			modify_id = #{modify_id},
			modify_date = SYSDATE
		WHERE
			portal_member_idx = #{portal_member_idx}
	</update>
	
	<delete id="deletePortalMember">
		DELETE FROM
			PORTAL_MEMBER
		WHERE
			portal_member_idx = #{portal_member_idx}
	</delete>
	
	<delete id="deletePortalMemberArr">
		DELETE FROM
			PORTAL_MEMBER
		WHERE
			portal_member_idx IN
			<foreach collection="portal_member_arr" item="i" open="(" close=")" separator=","> 
			#{i}
			</foreach>
	</delete>
	
	<select id="getPortalMemberLogin" resultType="kr.go.gbelib.app.cms.module.portalMember.PortalMember">
		SELECT
			portal_member_idx, agency_name, agency_id, agency_password,
			auth_group, library_code, last_connect, add_id, add_date
		FROM
			PORTAL_MEMBER
		WHERE
			agency_id = #{agency_id}
		AND	agency_password = #{agency_password}
	</select>
	
	<update id="addLastLogin">
		UPDATE
			PORTAL_MEMBER
		SET
			last_connect = SYSDATE
		WHERE
			agency_id = #{agency_id}
	</update>
	
	<select id="getPortalMemberMySQL" resultType="map">
		SELECT
			*
		FROM
			MEMBER
	</select>
	
	<insert id="addMyGration">
		INSERT INTO
			PORTAL_MEMBER
		(
			portal_member_idx, agency_name, agency_id, agency_password,
			auth_group, library_code, last_connect, add_id, add_date, modify_id, modify_date
		)
		VALUES
		(
			#{portal_member_idx}, #{agency_name}, #{agency_id}, #{agency_password},
			#{auth_group}, #{library_code}, #{last_connect}, #{add_id}, #{add_date}, #{modify_id}, #{modify_date}
		)
	</insert>
	
</mapper>