<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.go.gbelib.app.cms.module.bringInLog.BringInLogDao">
	
	<insert id="addLog">
		<selectKey keyProperty="log_idx" resultType="int" order="BEFORE">
			SELECT NVL(MAX(log_idx),0)+1 FROM BRINGIN_LOG
		</selectKey>
		INSERT INTO BRINGIN_LOG (
			log_idx, manage_code, member_id, member_name, birthday,
			cell_phone, ip, log_date
		) VALUES (
			#{log_idx}, #{manage_code}, #{member_id}, #{member_name}, #{birth_day},
			#{cell_phone}, #{add_ip}, SYSDATE
		)
	</insert>
	
</mapper>